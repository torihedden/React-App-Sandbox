import React from 'react';

const userOutput = (props) => {
    return (
        <div>
            <p>Update username: {props.userName}</p>
        </div>
    )
}

export default userOutput;